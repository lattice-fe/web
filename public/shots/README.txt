Landing-page carousel screenshots — real captures from the Lattice desktop app.

Referenced by the SHOTS array in src/app/page.tsx. To swap one, drop a new PNG
here with the same filename (app window, ~16:9). Current set:
  explorer, split-preview, watson, tab-preview, keep, home
